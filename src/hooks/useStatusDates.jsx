import { useState } from "@wordpress/element";

const useStatusDates = (status) => {
    // Evaluate if this is the right way of doing it or if it is better with a helper function that is not a hook
}

export default useEvents;